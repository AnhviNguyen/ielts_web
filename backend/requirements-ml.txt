# ML / audio — PyTorch CPU installed separately in Dockerfile (layer 1)
transformers>=4.41.0
faster-whisper>=1.1.0
pydub>=0.25.1
# SpeechBrain optional (~350 MB). Set SPEECHBRAIN_ENABLED=true to enable G2P;
# default fallback: g2p-en + CMU dict (see phoneme_recognizer.py).
# speechbrain>=1.0.0
sentencepiece>=0.2.0
g2p-en>=2.1.0
nltk>=3.8.1
numpy>=1.26.0
pandas>=2.0.0
# Optional — linear fallback trainer used if import fails (forecast/trainer.py)
neuralprophet>=0.8.0
scikit-learn>=1.3.0
joblib>=1.3.0
youtube-transcript-api>=0.6.2
yt-dlp>=2025.4.30
curl_cffi>=0.7.0
